/* 
const axios = require('axios')

export const loginCheck = () => {
  return axios.request({
    url: '/loginCheck',
    method: 'post',
    data: {
        email: email,
        password: password
      },
    timeout:1500
  })
} */